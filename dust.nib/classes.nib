{
    IBClasses = (
        {
            ACTIONS = {chooseLogo = id; closeSheet = id; defaultLogo = id; noLogo = id; }; 
            CLASS = Dust; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                coloursBox = id; 
                contrastBox = id; 
                diamondButton = id; 
                directionalButton = id; 
                hexButton = id; 
                logoImage = id; 
                motesSlider = id; 
                screensBox = id; 
                sizeSlider = id; 
                squareButton = id; 
                tailsBox = id; 
                window = id; 
            }; 
            SUPERCLASS = ScreenSaverView; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = ScreenSaverView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}